<?xml version="1.0" encoding="utf-8"?>
<!-- Atom generated by Buckley v0.2.2 (from buckley.cache.{{ static.src }}) -->
<feed xmlns="http://www.w3.org/2005/Atom">
	<id>{{ buckley.homepage }}</id> 
	<title>{{ buckley.title }}</title> 
	<link>{{ buckley.homepage }}</link> 
	<updated>{{ pubdate|isodate }}</updated>
	<built>{{ now|isodate }}</built>
	<generator>Buckley v0.2.3</generator>
	<author>
		<name>{{ buckley.owner }}</name>
	</author>
	{% for post in posts %}
	<entry> 
		<title>{{ post.title }}</title> 
		<link href="{{ post.url }}?src=feed" />
		<id>{{ post.url }}?src=feed</id>
		<author>
			<name>{{ post.author.nickname() }}</name>
			<email>{{ post.author.email() }}</email>
		</author>
		<published>{{ post.pubdate|isodate }}</published> 
		<updated>{{ post.updated|isodate }}</updated> 
		{% if buckley.feed_full %}
			<summary><![CDATA[{{ post.content_html }}]]></summary>
		{% else %}
			<summary><![CDATA[{{ post.excerpt_html }}<p><a href="{{ post.url }}">Continue reading</a></p>]]></summary>
		{% endif %}
	</entry>
	{% endfor %}
</feed>
